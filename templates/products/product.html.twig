{% extends 'base.html.twig' %}

    {% block stylesheets %}
        {{ parent() }}
        <link rel="stylesheet" href="{{ asset('css/product.css') }}">
        <link rel="stylesheet" href="{{ asset('fonts/font-awesome.min.css') }}">
    {% endblock %}

{% block body %}
    <div class="product-product container">
        <ul class="breadcrumb">
            <li><a href="{{ path('homepage') }}" class="px-2"><i class="fas fa-home"></i></a> »<li>
            <li><a href="{{ path('categories', {name: product.category.name}) }}" class="px-2">{{ product.category.name }}</a>»<li>
            <li class="px-2">{{ product.name }}<li>
        </ul>
        <div class="row">
            <div id="content" class="col-sm-12">
                <div class="row">
                    <div class="col-sm-6 col-lg-7 product_page-left">
                        <div class="product-gallery">
                                <div class="row px-3">
                                    <div id="carouselExampleIndicators" class="carousel my-4" data-ride="carousel">
                                        <ol class="carousel-indicators">
                                            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                                            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                                            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
                                            <li data-target="#carouselExampleIndicators" data-slide-to="3"></li>
                                        </ol>
                                        <div class="carousel-inner" role="listbox">
                                            <div class="carousel-item active">
                                                <img class="d-block img-fluid img-full" src="{{ asset('images/product-1a.png' | imagine_filter('thumb_product_info')) }}">
                                            </div>
                                            <div class="carousel-item">
                                                <img class="d-block img-fluid" src="{{ asset('images/product-2a.png' | imagine_filter('thumb_product_info')) }}">
                                            </div>
                                            <div class="carousel-item">
                                                <img class="d-block img-fluid" src="{{ asset('images/product-3a.png' | imagine_filter('thumb_product_info')) }}">
                                            </div>
                                            <div class="carousel-item">
                                                <img class="d-block img-fluid" src="{{ asset('images/product-4a.jpg' | imagine_filter('thumb_product_info')) }}">
                                            </div>
                                        </div>
                                        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                            <span class="sr-only">Previous</span>
                                        </a>
                                        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                            <span class="sr-only">Next</span>
                                        </a>
                                        <div class="cart-button d-none d-lg-block">
                                            <button type="button" class="btn-icon" data-toggle="tooltip" onclick="" data-original-title="Add to Wish List"><i class="fas fa-heart"></i></button>
                                            <button type="button" class="btn-icon" data-toggle="tooltip" onclick="" data-original-title="Compare this Product"><i class="fas fa-balance-scale"></i></button>
                                        </div>
                                    </div>
                                </div>
                        </div>
                    </div>
                    <div class="col-sm-6 col-lg-5 product_page-right">
                        <div class="general_info product-info">
                            <h2 class="product-title">{{product.name}}</h2>
                            <!-- Prodyuct rating status -->
                            <ul class="list-unstyled product-section">

                                <li class="mb-2"><strong>Brand:</strong>
                                    <a href="#">{{product.brand}}</a>
                                </li>

                                <li class="mb-2">
                                    <strong>Country:</strong>
                                    <a href="#">{{product.country}}</a>
                                </li>

                                <li class="mb-2">
                                    <strong>Warranty:</strong>
                                    <span>{{product.warranty}}</span>
                                </li>

                                <li class="mb-2">
                                    <strong>Availability:</strong>
                                    <span>In Stock</span>
                                    {#<span class="{{stock_status <= 0 ? 'out-stock' : 'stock'}}">In Stock</span>#}
                                </li>
                            </ul>
                            <div class="rating-section product-rating-status">
                                    <div class="rating">
                                        {% for i in 1..5 %}
                                            <i class="far fa-star"></i>
                                        {% endfor %}
                                        {#<span class="fa fa-stack"><i class="material-icons-star star fa-stack-1x"></i></span>#}
                                        <span class="review-link review-link-show">
									<a href="#">Comments</a>
								</span>
                                        <span class="review-link review-link-write">
									<a href="#">Add your comment</a>
								</span>
                                    </div>
                            </div>
                        </div>
                        <div id="product">
                            {#<!-- Add to cart form -->#}
                            <div class="form-group form-horizontal">
                                <div class="form-group">
                                    <div class="col-sm-12">
                                        <div class="quantity">
                                            <label class="control-label" for="input-quantity">Кількість</label>
                                            {#<a class="counter counter-minus fa fa-caret-down" href='#'></a>#}
                                            <input type="text" name="quantity" maxlength="2" min="1" value="1" size="2" id="input-quantity" class="form-control"/>
                                            <input type="hidden" name="product_id" value="{{product.id}}"/>
                                            {#<a class="counter counter-plus fa fa-caret-up" href='#'></a>#}
                                        </div>
                                    </div>
                                </div>
                            </div>
                                <div class="price-section">
                                    {#{% if (not special) %}#}
                                        {#<span class="price-new">{{price}}</span>#}
                                    {#{% else %}#}
                                        {#<span class="price-old">{{price}}</span>#}
                                    {#{% endif %}#}
                                    <span class="price-old">$10000</span>
                                    <span class="price-new">${{product.unitprice}}</span>

                                </div>

                            <button type="button" id="button-cart" class="btn-primary"><i class="fas fa-shopping-cart"></i><span class="px-2">Add to cart</span></button>
                            <div class="d-lg-none d-xl-none cart-button">
                                <button class="product-btn" type="button"><i class="fas fa-heart"></i><span>Wish List</span></button>
                                <button class="product-btn" type="button"><i class="fas fa-balance-scale"></i></i><span>Compare Product</span></button>
                            </div>

                        </div>
                    </div>

                </div>

            </div>
        </div>

    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
